# Suppress peer dependency warnings for React 19 compatibility
# These packages work fine with React 19 but haven't updated their peerDependencies yet
strict-peer-dependencies=false
auto-install-peers=true
