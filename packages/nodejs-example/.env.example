# FHEVM Node.js Example - Environment Configuration

# Hardhat Node RPC URL
# Default Hardhat node runs on http://localhost:8545
RPC_URL=http://localhost:8545

# Private Key (Hardhat Test Account)
# Use one of Hardhat's default test accounts (when you run `pnpm hardhat node`)
# Example: Account #0 from Hardhat startup
# WARNING: Never use real private keys with funds in examples or commit to git!
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Contract address is automatically loaded from deployedContracts.ts
# Run `pnpm deploy:localhost` to deploy and generate contract addresses
